#version 450 core
layout(local_size_x = 1, local_size_y = 1) in;
layout(binding = 1, rgba16f) uniform image2D myImage;

void main() {
    vec4 a = imageLoad(myImage, ivec2(gl_GlobalInvocationID));
    imageStore(myImage, ivec2(gl_GlobalInvocationID.xy), a.wxyz);
}