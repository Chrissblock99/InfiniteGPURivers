#version 450 core
layout(local_size_x = 1, local_size_y = 1) in;
layout(binding = 1, r16f) uniform image2D waterMap;

void main() {
    vec4 a = imageLoad(waterMap, ivec2(gl_GlobalInvocationID));
    imageStore(waterMap, ivec2(gl_GlobalInvocationID.xy), a + 1);
}